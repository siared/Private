# 레퍼런스 시안

브랜드 템플릿 이미지를 여기에 넣는다. 최소 구성 권장:

- `list-*.png` — 목록/테이블 페이지 1장
- `form-*.png` — 등록/수정 폼 페이지 1장
- `dashboard-*.png` — 대시보드 1장 (있다면)

이미지를 넣은 뒤 Claude에게 "images의 시안에서 패턴을 추출해서
docs/layout-patterns.md 와 tokens/tokens.md 의 TODO를 채워줘" 라고 요청하면 된다.
Figma를 쓰는 경우 이미지 대신 Figma 링크를 줘도 된다.
